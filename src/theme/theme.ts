import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#634832",
    },
    secondary: {
      main: "#dbc1ac",
    },
  },
});

export default theme;
